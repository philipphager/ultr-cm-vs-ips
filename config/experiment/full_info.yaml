# @package _global_
defaults:
  - override /model: ips

model:
  name: "Full Information"
  position_bias: 0
  learning_rate: 0.0001
  optimizer: adam

simulation:
  simulator:
    user_model:
      position_bias: 0
      noise: 0
